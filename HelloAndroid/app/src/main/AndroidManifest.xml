<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hb2006.helloandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MemberActicity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
           <activity android:name=".MainActivity"
            android:label="@string/app_name"
            >

        </activity>
        -->
        <activity
            android:name=".Join"
            android:label="@string/title_activity_join"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Write"
            android:label="글쓰기"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Board"
            android:label="게시판"
            android:theme="@style/AppTheme.NoActionBar" />

        <provider
            android:name=".MemberProvider"
            android:authorities="com.example.hb2006.helloandroid.MemberProvider" />

        <activity
            android:name=".CRUDSameple"
            android:label="@string/title_activity_crudsameple"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
